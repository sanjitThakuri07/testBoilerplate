1. When changing the value set on the form
    a. we have used the local state for improving performances
    b. so, when changing please look at the formNode component where the logic rendering is being checked in that component we have passed 
        the value of local state